#include<stdio.h>
#include<string.h>

int main(){
   // char st[] = "Krishna";

    //printf("%d", strlen(st)); // Counts the number of characters in strings excluding the null character

    //char copy[20];
    //strcpy(copy, st); // Copies and pastes one string to another string
    //printf("%s \n", copy); 

    //char first[10] = "My name is";
   // char second[] = "Krishna";

   // strcat(first, second); // Concatenates two strings

   // printf("%s", first);

   int a = strcmp("Hello", "World");
   printf("%d", a);
 
}